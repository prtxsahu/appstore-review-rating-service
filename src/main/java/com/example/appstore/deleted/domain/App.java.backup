// MOVED TO: com.example.appstore.app.domain.App
// This file has been moved to the new feature-based structure
// TODO: Remove this file after confirming the refactoring is complete

/*
package com.example.appstore.domain;

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import software.amazon.awssdk.enhanced.dynamodb.mapper.annotations.DynamoDbAttribute;
import software.amazon.awssdk.enhanced.dynamodb.mapper.annotations.DynamoDbBean;
import software.amazon.awssdk.enhanced.dynamodb.mapper.annotations.DynamoDbPartitionKey;

import java.math.BigDecimal;
import java.time.Instant;

/**
 * App entity representing an application in the app store.
 * Note: total_sum and total_count are moved to Aggregate table as per updated design.
 */
/*
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
@DynamoDbBean
public class App {
    // Implementation moved to com.example.appstore.app.domain.App
}
*/